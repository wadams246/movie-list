{
  "name": "movie-list",
  "version": "0.0.0",
  "description": "A movie list code challenge",
  "devDependencies": {
    "clean-webpack-plugin": "^0.1.17",
    "html-webpack-plugin": "^2.30.1",
    "http-server": "^0.9.0",
    "jasmine-core": "^2.4.1",
    "karma": "^0.13.22",
    "karma-chrome-launcher": "^0.2.3",
    "karma-firefox-launcher": "^0.1.7",
    "karma-jasmine": "^0.3.8",
    "karma-junit-reporter": "^0.4.1",
    "webpack": "^3.8.1"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "npm run clean; webpack; http-server -a localhost -p 8000 -c-1 ./app",
    "pretest": "npm install",
    "test": "karma start karma.conf.js",
    "test-single-run": "karma start karma.conf.js --single-run",
    "clean": "rm -r app/scripts/dist/*"
  },
  "dependencies": {
    "angular": "~1.4.14",
    "angular-route": "~1.4.14"
  }
}
